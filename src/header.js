function Header(){
return(
    <section className="top">
    <h1 id="pageTitle">Sophie and Joey are getting married!</h1>
      <h6 id="subTitle">we don't know where or when, but it is happening</h6>
    </section>  
)
}






export default Header;